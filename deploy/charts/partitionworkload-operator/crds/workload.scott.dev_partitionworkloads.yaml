---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: partitionworkloads.workload.scott.dev
spec:
  group: workload.scott.dev
  names:
    kind: PartitionWorkload
    listKind: PartitionWorkloadList
    plural: partitionworkloads
    singular: partitionworkload
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: PartitionWorkload is the Schema for the partitionworkloads API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of PartitionWorkload
            properties:
              partition:
                description: |-
                  Partition describes the number of pods that are at the latest pod template revision
                  when revision is made to spec.Template. The remaining rest of the pods
                  (spec.Replicas - spec.Partition) can be of any version (but not the latest version).
                  Note that there can be more than two versions of pods. When the desired state is reached,
                  exactly spec.Partition number of pods have the latest
                  version. It defaults to spec.Replicas by controller logic.
                  If it is larger than spec.Replicas, it is clamped to be the same as spec.Replicas.
                format: int32
                minimum: 0
                type: integer
              replicas:
                default: 1
                description: |-
                  Replicas is the desired number of replicas of the given Template.
                  These are replicas in the sense that they are instantiations of the
                  same Template.
                  If unspecified, defaults to 1.
                format: int32
                minimum: 0
                type: integer
              selector:
                description: |-
                  Selector is a label query over pods that should match the replica count.
                  It must match the pod template's labels.
                properties:
                  matchExpressions:
                    description: matchExpressions is a list of label selector requirements.
                      The requirements are ANDed.
                    items:
                      description: |-
                        A label selector requirement is a selector that contains values, a key, and an operator that
                        relates the key and values.
                      properties:
                        key:
                          description: key is the label key that the selector applies
                            to.
                          type: string
                        operator:
                          description: |-
                            operator represents a key's relationship to a set of values.
                            Valid operators are In, NotIn, Exists and DoesNotExist.
                          type: string
                        values:
                          description: |-
                            values is an array of string values. If the operator is In or NotIn,
                            the values array must be non-empty. If the operator is Exists or DoesNotExist,
                            the values array must be empty. This array is replaced during a strategic
                            merge patch.
                          items:
                            type: string
                          type: array
                          x-kubernetes-list-type: atomic
                      required:
                      - key
                      - operator
                      type: object
                    type: array
                    x-kubernetes-list-type: atomic
                  matchLabels:
                    additionalProperties:
                      type: string
                    description: |-
                      matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                      map is equivalent to an element of matchExpressions, whose key field is "key", the
                      operator is "In", and the values array contains only "value". The requirements are ANDed.
                    type: object
                type: object
                x-kubernetes-map-type: atomic
              template:
                description: Template describes the pods that will be created.
                x-kubernetes-preserve-unknown-fields: true
            required:
            - selector
            - template
            type: object
          status:
            description: status defines the observed state of PartitionWorkload
            properties:
              collisionCount:
                description: |-
                  CollisionCount is the count of hash collisions for the PartitionWorkload. The PartitionWorkload controller
                  uses this field as a collision avoidance mechanism when it needs to create the name for the
                  newest ControllerRevision.
                format: int32
                type: integer
              conditions:
                description: Conditions represents the latest available observations
                  of a PartitionWorkload's current state.
                items:
                  properties:
                    lastTransitionTime:
                      description: Last time the condition transitioned from one status
                        to another.
                      format: date-time
                      type: string
                    lastUpdateTime:
                      description: Last time the condition is updated.
                      format: date-time
                      type: string
                    message:
                      description: A human readable message indicating details about
                        the transition.
                      type: string
                    reason:
                      description: The reason for the condition's last transition.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: Type of PartitionWorkload condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              currentRevision:
                description: CurrentRevision, if not empty, indicates the current
                  revision version of the PartitionWorkload.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the most recent generation observed for this PartitionWorkload. It corresponds to the
                  PartitionWorkload's generation, which is updated on mutation by the API Server.
                format: int64
                minimum: 0
                type: integer
              readyReplicas:
                description: ReadyReplicas is the number of Pods created by the PartitionWorkload
                  controller
                format: int32
                minimum: 0
                type: integer
              updateRevision:
                description: UpdateRevision, if not empty, indicates the latest revision
                  of the PartitionWorkload.
                type: string
              updatedReplicas:
                description: |-
                  UpdatedReplicas is the number of Pods created by the PartitionWorkload controller from the PartitionWorkload version
                  indicated by updateRevision.
                format: int32
                minimum: 0
                type: integer
            required:
            - readyReplicas
            - updatedReplicas
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
